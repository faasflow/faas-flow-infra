apiVersion: v1
kind: Pod
metadata:
  name: minio-test-connection"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['minio:9000']
  restartPolicy: Never
